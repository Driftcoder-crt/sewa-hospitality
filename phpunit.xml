<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!-- Test suite runs fully in-memory: sqlite :memory:, array cache
             and sessions, sync queue, array mailer. No service or network
             dependency — CI gate 1 (03-technical-specs/13-testing-qa.md §3)
             must stay green on a bare container. -->
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <!-- Fixed, TEST-ONLY application key (base64 of
             "sewa-test-key-0123456789abcdefgh" — exactly 32 bytes for
             AES-256-CBC). Deterministic so encrypted fixtures/cookies are
             reproducible. NEVER copy into .env or any real environment. -->
        <env name="APP_KEY" value="base64:c2V3YS10ZXN0LWtleS0wMTIzNDU2Nzg5YWJjZGVmZ2g="/>
    </php>
</phpunit>
